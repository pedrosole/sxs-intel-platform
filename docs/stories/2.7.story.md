# Story 2.7 — Fluxo Integrado Calendário → Design

**Epic:** 2 — Design Studio
**Tipo:** Feature
**Prioridade:** MEDIUM
**Estimativa:** P (pequena)
**Depende de:** 2.5
**Status:** Done

---

## Descrição

Conectar o calendário editorial ao Design Studio. Quando uma peça é aprovada no calendário, ela aparece automaticamente na fila do Design Studio. Botões de ação conectam as duas telas.

## Acceptance Criteria

- [x] Calendário (`/calendario/[token]`): botão "Criar Visual" em peças aprovadas
  - Redireciona para `/clientes/[slug]/design?piece={pieceId}`
  - Botão só aparece se `status = 'aprovado'`
  - Também mostra "Abrir no Design Studio" para `em_design`
- [x] Página `/clientes`: coluna "Visual" com contadores
  - Badge `X/Y visuais` com progresso visual
- [x] Design Studio: deep link funciona
  - Se `?piece={id}` na URL → abre direto no painel dessa peça
- [x] Status sincronizado:
  - Calendário mostra status visual da peça (ícones diferenciados)
  - `aprovado` = aguardando visual (verde + botão Criar Visual)
  - `em_design` = em produção visual (azul)
  - `visual_aprovado` = visual pronto (verde)
  - `exportado` = PNG gerado (primary + ícone download)
- [x] Navegação de volta: Design Studio → link para calendário do cliente

## Scope

**IN:** Botões de ação, deep links, status visual sincronizado, contadores
**OUT:** Notificações, automação de fluxo (auto-criar visual ao aprovar)

## Notas Técnicas

- Status expandido de `calendar_pieces` (migration na story 2.1)
- Deep link via `useSearchParams` + `useEffect` auto-select
- Contadores via query agregada com `count: "exact"` no Supabase

## File List

- `src/app/api/calendario/[token]/route.ts` — Adicionado `slug` no select de clients
- `src/app/calendario/[token]/page.tsx` — Botão "Criar Visual", status visuais expandidos, StatusBadge/StatusDot
- `src/app/clientes/[slug]/design/page.tsx` — Deep link `?piece=`, link para calendário
- `src/app/clientes/page.tsx` — Contador visual X/Y
- `src/app/api/clientes/route.ts` — Query contadores visual_total/visual_done
